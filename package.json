{
  "name": "icloud-code-vault",
  "version": "1.0.0",
  "private": true,
  "description": "Private multi-provider verification-code inbox for authorized mailboxes",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "worker": "node src/worker.js",
    "check": "node --check src/server.js && node --check src/worker.js && node --check src/lib.js && node --check src/extract.js && node --check src/totp.js && node --check public/admin.js && node --check public/query.js && node --check public/login.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "html-to-text": "^10.0.0",
    "imapflow": "^1.0.181",
    "lucide": "^0.468.0",
    "mailparser": "^3.7.2",
    "otplib": "^12.0.1",
    "pg": "^8.13.1",
    "qrcode": "^1.5.4"
  }
}
